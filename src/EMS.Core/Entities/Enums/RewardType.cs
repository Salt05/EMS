namespace EMS.Core.Entities.Enums;

public enum RewardType
{
    TrainingPoint = 1,   // Điểm rèn luyện (Trường học)
    PhysicalGift = 2,    // Quà tặng vật lý (Áo phông, bình giữ nhiệt...)
    Voucher = 3,         // Mã giảm giá / Giftcode
    Certificate = 4,     // Giấy chứng nhận điện tử
    InternalCoin = 5     // Điểm thưởng nội bộ (Công ty)
}
